# Low-rank toolbox

(!) There is nothing to run in this folder.

It contains the low-rank matrix containers (`LowRankMatrix`, `QuasiSVD`,
`SVD`, `QR`) and the Gram-Schmidt orthogonalization routines (classical,
modified, and randomized) used by the sketch DLRA experiments.
